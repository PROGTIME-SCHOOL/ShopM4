@model ShopM4.Models.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Главная страница";
}

<div class="container">
    @foreach (var prod in Model.Products)
    {
        <partial name="_ProductCard" model="prod" />
    }
</div>
